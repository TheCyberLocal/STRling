# The packaged language server has no external Python package dependencies.
# Its bounded pygls/lsprotocol-compatible transport is copied from repository
# source by package_extension.py. Build dependencies are governed by
# package-lock.json; canonical semantics execute in the packaged Rust binaries.
