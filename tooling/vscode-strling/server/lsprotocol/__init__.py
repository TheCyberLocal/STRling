"""Bundled ``lsprotocol`` types for the VS Code extension.

The runtime imports ``lsprotocol.types`` directly; the package initializer is
kept empty so static analysis does not need to resolve re-exported symbols.
"""
