import XCTest
@testable import STRling

/// Test suite for STRling Swift binding
///
/// This test suite validates the core functionality of the STRling library,
/// including parsing, compilation, and error handling.
final class STRlingTests: XCTestCase {
    
    /// Placeholder test to ensure the test target builds correctly
    func testExample() throws {
        // This is a placeholder test that will be replaced with actual tests in Task 2
        XCTAssertEqual(STRling.version, "3.0.0-alpha")
    }
}
