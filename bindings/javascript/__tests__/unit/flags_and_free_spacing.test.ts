/**
 * @file flags_and_free_spacing.test.ts
 *
 * This file has been migrated to the shared spec in `tests/spec/`.
 * See `tooling/js_to_json_ast/fixtures/` for the source patterns.
 */

describe("Legacy Migration", () => {
    test("tests have been migrated to shared spec", () => {
        expect(true).toBe(true);
    });
});
