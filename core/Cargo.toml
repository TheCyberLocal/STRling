[package]
name = "strling-kernel"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
license = "MIT"
description = "Canonical STRling compiler kernel"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "strling_kernel"
path = "src/lib.rs"

