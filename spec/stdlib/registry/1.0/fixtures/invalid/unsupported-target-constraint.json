{
    "case_id": "unsupported-target-constraint",
    "expected_rule": "target.constraint.supported",
    "kind": "strling.stdlib-registry-invalid-case",
    "operations": [
        {
            "op": "replace",
            "path": "/helpers/0/targeting/constraint_ids/0",
            "value": "regex.unsupported-feature"
        }
    ],
    "registry_reference": "spec/stdlib/registry/1.0/registry.json"
}
