schema_version: 1
title: Static analysis, warning discipline, and suppression hardgates
status: in_progress
objective: Establish deterministic defect-focused lint, type/static analysis, fatal warning policy, and governed suppressions without changing STRling semantics or public contracts.
starting_sha: bb334b0980327b8f3599cd6da0562022c365641a
branch: architecture/v4
policy:
    static_analysis: governance/static-analysis.json
    toolchain: toolchain.json
checkpoints:
    - name: Static-quality contract and baseline
      status: passed
      accomplishment:
          - Assigned an explicit format, lint, type/static, warning, and test disposition to all 19 active targets.
          - Selected canonical native or ecosystem-standard analyzers and recorded their version authority or exact unresolved transition.
          - Inventoried 92 tracked functional suppression directives and separated transition debt from suspected product defects.
          - Recorded reproducible pre-enforcement findings without changing product source.
      verification:
          - command: ./strling check
            result: Passed canonical formatting, hygiene, TypeScript strict analysis, and 963 TypeScript tests.
          - command: ./strling lint all --json; ./strling typecheck all --json
            result: Existing configured capabilities passed; 18 targets executed no lint analyzer and remained explicit.
          - command: Strict native analyzer baseline described in governance/static-analysis.json
            result: Clean mature paths and bounded findings were reproduced across all active ecosystems; missing tools were recorded as unavailable or unresolved.
          - command: python3 -m json.tool toolchain.json; python3 -m json.tool governance/static-analysis.json; ./strling check; git diff --check
            result: Policies parsed, the established check remained green with 963 TypeScript tests, and patch integrity passed.
      commit:
          subject: "docs(quality): define static analysis and warning policy"
          sha: a2bd48f8ead42bd7d9dfe154e0074d7f3fda6f08
    - name: Canonical lint and static-analysis integration
      status: passed
      accomplishment:
          - Extended capability states with enforced, not_yet_enforceable, and unavailable while preserving explicit legacy formatting transitions.
          - Added canonical Ruff, .NET, Go vet, Java, Kotlin, Perl, PHP, Ruby, and LSP TypeScript commands with component scoping.
          - Added repository-resolved TypeScript version probes for both the binding and LSP lock files.
          - Added structured capability metadata and propagated analyzer exit status without discarding output.
      verification:
          - command: python3 -m unittest tooling.tests.test_quality
            result: 35 tests passed, including lint/typecheck success and failure, unavailable analyzer, transitional capability, scoping, and aggregate propagation cases.
          - command: ./strling lint lsp --json; ./strling typecheck lsp --json; ./strling lint go --json; ./strling lint perl --json; ./strling lint php --json
            result: All representative positive commands passed and reported component, operation, result status, capability, command, environment, and reason.
          - command: ./strling lint repository --json; ./strling lint python --json
            result: Both known Ruff baselines returned nonzero structured failures; no diagnostics were discarded.
          - command: ./strling typecheck typescript --json
            result: Strict TypeScript checking passed with the repository-resolved 5.9.3 analyzer probe.
      commit:
          subject: "feat(quality): integrate canonical static analysis"
          sha: 8602659a75d5a371e4b3ba14610cadf690b8b95a
    - name: Warning and suppression governance
      status: passed
      accomplishment:
          - Made C#, F#, Dart, Java, Kotlin, Rust, Swift, Perl, and Ruby warning paths fatal through compiler/analyzer flags or trustworthy wrapper handling.
          - Added token-aware tracked-source suppression detection for Python plus directive detection for every active source family.
          - Matched all 92 existing directives to four exact-path policy waivers; new or moved directives, count drift, invalid records, and expired bounded waivers fail.
          - Kept the C truncation and TypeScript callable-Pattern findings recorded as product defects rather than silencing them.
      verification:
          - command: python tooling/static_analysis.py suppressions
            result: Passed with 92 managed directives, four waivers, and zero findings after staging the scanner and its tests.
          - command: python -m unittest tooling.tests.test_static_analysis
            result: Seven tests passed for managed, unmanaged, expired, invalid, broad directive, repository baseline, and warning-to-failure cases.
          - command: python -m json.tool toolchain.json; python -m json.tool governance/static-analysis.json; python -m py_compile tooling/quality.py tooling/static_analysis.py
            result: Governance and implementation parsed successfully.
          - command: Strict warning command baselines from checkpoint one plus explicit fatal flags in toolchain.json, pom.xml, and build.gradle.kts
            result: Clean compiler paths are fatal now; Dart, Kotlin, Ruby, and Rust remain intentionally red until behavior-neutral checkpoint-four remediation.
      active_waivers:
          - WVR-STATIC-PYTHON-001 expires 2027-02-01
          - WVR-STATIC-TYPESCRIPT-PATTERN-001 expires 2027-01-15 and records suspected product behavior
          - WVR-STATIC-RUST-ALLOW-001 expires 2026-12-31
          - WVR-STATIC-SHELL-INTEROP-001 is a narrow permanent interoperability exception
      commit:
          subject: "feat(quality): govern warnings and analyzer suppressions"
          sha: 5058bbdd20816d8827451f73978fc2e29a62044e
    - name: Behavior-neutral static-quality remediation
      status: passed
      accomplishment:
          - Removed three unused Python imports, two neutral Dart findings, and eight unused Ruby test-local assignments.
          - Added four declaration-scoped Kotlin serialization opt-ins and enabled the already-configured fatal warning path without runtime changes.
          - Removed one generated Rust import, four unused Rust imports, six obsolete allow attributes, and one private unreferenced cursor helper.
          - Removed the project-owned CMake FetchContent timestamp warning while leaving the transitive dependency warning explicit.
          - Reduced governed suppressions from 92 to 86 and retired WVR-STATIC-RUST-ALLOW-001.
      verification:
          - command: ./strling lint repository --json; ./strling lint python --json; ./strling lint dart --json; ./strling lint kotlin --json; BUNDLER_VERSION=2.4.20 ./strling lint ruby --json; ./strling typecheck rust --json
            result: Every remediated analyzer passed; repository suppression audit reported 86 managed directives, three waivers, and zero findings.
          - command: ./strling test python --json; ./strling test dart --json; ./strling test kotlin --json; BUNDLER_VERSION=2.4.20 ./strling test ruby --json; ./strling test rust --json
            result: All affected canonical binding test commands passed.
          - command: python3 -m unittest tooling.tests.test_quality tooling.tests.test_static_analysis; python3 -m pytest tooling/tests/test_sync_versions.py -q
            result: 42 quality/suppression tests and six version-sync tests passed.
          - command: CMAKE_COMPILE_WARNING_AS_ERROR=ON C++ configure and build
            result: The project-owned CMake warning was eliminated and compilation passed; one explicitly recorded nlohmann/json deprecation warning remains transitive.
          - command: committed and remediated bindings/ruby/test/interaction_test.rb
            result: Both produced 32 runs, 24 assertions, four failures, and eight errors; the pre-existing behavioral defects are separately deferred.
          - command: ./strling format --check; ./strling hygiene; git diff --check
            result: Canonical formatting and hygiene passed; patch integrity passed.
      commit:
          subject: "refactor(types): remove behavior-neutral static analysis violations"
          sha: pending
    - name: Hardgate integration and certification
      status: pending
behavior_changed: No STRling language/compiler semantics intentionally changed.
readiness: pending
